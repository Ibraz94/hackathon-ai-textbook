name: CI

on:
  push:
    branches:
      - master
      - 001-robotics-textbook-setup
  pull_request:
    branches:
      - master
      - 001-robotics-textbook-setup

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
        working-directory: ./docusaurus-site
      - name: Build Docusaurus site
        run: npx --yes docusaurus build
        working-directory: ./docusaurus-site

  deploy:
    needs: build
    if: github.ref == 'refs/heads/gh-pages' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
        working-directory: ./docusaurus-site
      - name: Build Docusaurus site
        run: npx --yes docusaurus build
        working-directory: ./docusaurus-site
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docusaurus-site/build
          publish_branch: gh-pages
